# Jetson backend contract

`JetsonExportBundle` records static model/policy checksums, batch size 1, input shape, and a versioned result schema. A future backend must identify its runtime and verify native precision execution before any device-specific claim enters a Pareto frontier.

Current server timing is fake-quant diagnostic data only and cannot support Jetson latency, energy, or low-bit speedup claims.
